import {NextPage} from "next";

const Page = (props: NextPage) => {
    return (
        <div>
            <h1 className={"text-xl underline"}>Index Page</h1>
        </div>
    )
}

export default Page
