---
import Layout from '@/layouts/Layout.astro';

export function getStaticPaths() {
  return [
    { params: { locale: 'en' } },
    { params: { locale: 'id' } },
  ];
}

const { locale } = Astro.params;
---

<Layout locale={locale}>
  <h1>Hello Astro — {locale}</h1>
</Layout>
